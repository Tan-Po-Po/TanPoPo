"use client";

import { Button } from "@/components";
import cl from "./cartButton.module.scss";

export const CartButton = () => {
  return (
    <Button className={cl.cartButton} variant="outlined">
      sdfsdf
    </Button>
  );
};
